import 'package:flutter/material.dart';

class SelectStudentsScreen extends StatelessWidget {
  const SelectStudentsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Container(
      height: 1000,color: Colors.red,
    );
  }
}
